# Uncomment the next line to define a global platform for your project
# platform :ios, '9.0'

target 'iOS-VIPER-Swift' do
  # Comment the next line if you don't want to use dynamic frameworks
  use_frameworks!

  pod 'Alamofire'
  pod 'SDWebImage'
  
  # Pods for iOS-VIPER-Swift

  target 'iOS-VIPER-SwiftTests' do
    inherit! :search_paths
    # Pods for testing
  end

  target 'iOS-VIPER-SwiftUITests' do
    # Pods for testing
  end

end
